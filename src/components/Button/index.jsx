import React from 'react';

const PrimaryButton = ({title}) => {
    return (
        <>
            <div>
                {title}
            </div>
        </>
    );
};

export default PrimaryButton;